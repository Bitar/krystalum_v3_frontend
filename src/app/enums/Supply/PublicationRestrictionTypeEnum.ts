/* This Enum defines the possible publication restriction types that can be used in the project. */
export enum PublicationRestrictionTypeEnum {
    REQUIRES_CREATIVE_APPROVAL = 'Requires creative approval',
    DOES_NOT_RUN_CAMPAIGNS = 'Does not run campaigns',
    META_RESTRICTIONS = 'meta_restrictions'
}